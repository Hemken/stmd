## Examples

Using \"{stata}\"
```{stata}
sysuse auto, clear
```

Text about `sysuse`.  Text about `stata`?

Using \"{s}\"
```{s}
summarize weight
```

Mean weight `{stata} %9.1f r(mean)` (using "{stata}")  
Mean weight `{s} %9.1f r(mean)` (using "{s}")

Using \"stata\" (no braces)
```stata
summarize price
```

Mean price `stata %9.1f r(mean)` (using "stata")

Using \"s\"
```s
summarize mpg
```

Mean mpg `s %9.1f r(mean)` (using "s")
