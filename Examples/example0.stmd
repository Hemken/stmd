An Example File
===============

Some preliminary text.

A code block, evaluted:
```stata
sysuse auto
summarize weight
```

In-line text: the mean weight is `stata %9.1f r(mean)` pounds.

A list:

- N = `s r(N)`
- mean = `s %9.1f r(mean)`
- sd = `s % 9.2f r(sd)`

A graph:
```stata
histogram weight
```

More text.
