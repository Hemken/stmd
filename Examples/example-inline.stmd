Inline Language Calls
===============

Some preliminary text.

A code block, setup:
```{stata}
sysuse auto
summarize weight
```

The mean weight is `s r(mean)`

```{stata}
return list
```

Using `s`, formatted

The mean weight is `s %6.1f r(mean)`.

Using ``stata`

The maximum weight is `stata r(max)`.

Using `{s}` and `{stata}`, and two calls per line.

The weight range is `{s} r(min)` to `{stata} r(max)`.
